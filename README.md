# Telegram Support Bot

> Этот проект представляет собой Telegram-бота для организации простейшей системы поддержки.
> Бот принимает сообщения пользователей (текст, фото, видео, аудио, документы, голосовые) и пересылает их в чат поддержки.
> Операторы поддержки отвечают на сообщения прямо в общем чате, используя функцию Reply, а бот автоматически перенаправляет ответ тому пользователю, от которого пришёл запрос.

*Это мой первый публичный репозиторий, поэтому не ругайте строго ✨*

---

<p align="center">
  <img src="https://img.shields.io/badge/Python-3.10+-blue.svg">
  <img src="https://img.shields.io/badge/Telegram%20Bot%20API-async-green.svg">
  <img src="https://img.shields.io/badge/License-MIT-lightgrey.svg">
  <img src="https://img.shields.io/badge/Database-SQLite-orange.svg">
</p>

---

## 🚀 Возможности

* **🔄 Двусторонняя связь:** Пересылка сообщений от пользователей в чат поддержки и обратно.
* **📁 Гибкая система топиков:** 
  - **Режим "Отдельный топик для каждого"** — автоматическое создание персонального топика для каждого пользователя с отображением статуса (🟢 открыт / 🔴 закрыт)
  - **Режим "Общий топик"** — все тикеты в одном топике (классический режим)
  - Переключение режимов через админ-панель без перезапуска бота
* **⚙️ Админ-панель:** Администраторы могут менять текст приветствия (/start) и справки (/help) прямо через бота, а также переключать режим работы топиков
* **⛔️ Блокировка пользователей**: Удобные инлайн-кнопки для бана/разбана пользователей прямо в интерфейсе чата.
* **🎫 Система тикетов**: Автоматическое создание уникального ID тикета для каждого пользователя.
* **📊 Управление статусами**: open/closed, команды `/close`, `/reopen`.
* **📈 Список тикетов**: `/open_tickets` — все открытые тикеты с деталями.
* **📤 Поддержка всех медиа**: Текст, фото, видео, документы, голосовые, аудио.
* **💾 База данных:** Использует SQLite для хранения связей сообщений (чтобы бот знал, кому отправлять ответ), черного списка и тексты настроек бота.

## 📋 Требования

* Python 3.8+
* Токен Telegram бота
* ID чата (группы), где будут находиться операторы поддержки
* **Для режима "Общий топик"**: ID топика (`SUPPORT_TOPIC_ID`)
* **Для режима "Отдельный топик для каждого"**: Чат должен быть супергруппой с включенными топиками (форум), бот должен быть администратором с правами `can_manage_topics`
* ID администраторов (для доступа к настройкам текстов)

## 🛠 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/gniloeapple/telegram-support-bot.git
cd telegram-support-bot

cp .env.example .env
nano .env
```

**Пример заполнения `.env`:**

```ini
TELEGRAM_TOKEN=123456789:AAH...             # Токен от BotFather
SUPPORT_CHAT_ID=-1001234567890              # ID группы поддержки (начинается с -100)
SUPPORT_TOPIC_ID=12345                      # Опционально: ID топика (для режима "Общий топик")
ADMINS=123456789,987654321                  # ID администраторов через запятую (без пробелов)
```

**Где взять данные:**

* **TELEGRAM_TOKEN**: Напишите [@BotFather](https://t.me/BotFather), создайте нового бота и скопируйте выданный токен.
* **SUPPORT_CHAT_ID**:
  1. Создайте группу в Telegram, добавьте туда вашего бота и сделайте его администратором.
  2. Используйте стороннего бота (например, `@GetMyIDBot` или перешлите сообщение в `@userinfobot`), чтобы узнать ID этой группы.
  3. Обязательно убедитесь, что ID начинается с `-100`.
* **SUPPORT_TOPIC_ID** (необязательно):
  - Нужен только для режима "Общий топик"
  - Включите топики в группе (Настройки → Топики)
  - Создайте топик для поддержки
  - Перешлите любое сообщение из этого топика боту `@userinfobot`, чтобы узнать `message_thread_id`
* **ADMINS**: Ваш цифровой ID в Telegram (можно узнать у `@userinfobot`).

### 2. Создание виртуального окружения (рекомендуется)

```bash
python -m venv venv
# Активация для Windows:
venv\Scripts\activate
# Активация для Linux/macOS:
source venv/bin/activate
```

### 3. Установка зависимостей

Установите необходимые библиотеки из файла `requirements.txt`:

```bash
pip install -r requirements.txt
```

### 4. Настройка группы поддержки

**Для режима "Отдельный топик для каждого":**
1. Преобразуйте группу в супергруппу (если ещё не сделано)
2. Включите топики: Настройки группы → Топики → Включить
3. Добавьте бота как администратора с правами управления топиками (`can_manage_topics`)

**Для режима "Общий топик":**
1. Создайте топик в группе (если используете топики)
2. Укажите `SUPPORT_TOPIC_ID` в `.env`

### 5. Запуск бота

```bash
python bot.py
```

## 📖 Как использовать

### Для пользователей

1. Пользователь запускает бота командой `/start`.
2. Пишет сообщение, отправляет фото или видео.
3. Бот пересылает это в чат поддержки (в отдельный топик или общий — в зависимости от настроек).

> Если пользователь заблокирован, бот будет игнорировать его сообщения.

### Для операторов поддержки

**В режиме "Отдельный топик для каждого":**
1. Для каждого нового пользователя создаётся отдельный топик с названием "🟢 Username" или "🟢 Имя"
2. В первом сообщении топика — информация о пользователе (ID, username, имя)
3. Все последующие сообщения от этого пользователя приходят в этот же топик
4. При закрытии тикета название топика меняется на "🔴 Username"

**В режиме "Общий топик":**
1. В чат поддержки приходят сообщения с полной информацией о пользователе
2. Первое сообщение от нового пользователя содержит детали тикета

**Для обоих режимов:**
1. Под каждым сообщением есть кнопка **❌ Заблокировать/Разблокировать**
2. Чтобы ответить пользователю, оператор должен сделать **Reply (Ответить)** на сообщение
3. Бот перешлёт ответ оператора обратно пользователю в личные сообщения

## 🤖 Команды бота

### Для пользователя (в ЛС бота)

* `/start` — приветственное сообщение и инструкция.
* `/help` — подсказка по работе поддержки и тикетов.

### Для администраторов (в ЛС бота)

Команда `/admin` открывает меню настроек:

* **✏️ Изменить приветствие** — задать текст, который видит пользователь при нажатии `/start`.
* **📝 Изменить информацию** — задать текст для команды `/help`.
* **🔄 Режим топиков** — переключение между режимами:
  - 📁 **Отдельный топик для каждого** — каждый пользователь получает персональный топик
  - 📂 **Общий топик** — все тикеты в одном топике

### Для операторов (в чате поддержки)

Команды должны вызываться в чате, ID которого указан в `SUPPORT_CHAT_ID`:

* `/open_tickets`
  Выводит список открытых тикетов с ID пользователя, username, именем и временем создания.

* `/close` (по реплаю на сообщение тикета)
  Закрывает тикет и отправляет пользователю уведомление «✅ Обращение завершено».
  В режиме "Отдельный топик" также меняет название топика на "🔴".

* `/reopen` (по реплаю)
  Меняет статус тикета обратно на open.
  В режиме "Отдельный топик" меняет название топика на "🟢".

* `/ticket` (по реплаю)
  Показывает краткую информацию: ID пользователя, статус тикета, **статус блокировки (Да/Нет)**, дата создания и обновления.

## 🎯 Режимы работы топиков

### 📁 Отдельный топик для каждого (per_user)

**Преимущества:**
- Каждый пользователь в отдельном топике — удобно для множества обращений
- Визуальная индикация статуса в названии топика (🟢/🔴)
- Информация о пользователе сразу в начале топика
- Лёгкий поиск по пользователям

**Требования:**
- Супергруппа с включенными топиками (форум)
- Бот — администратор с правами `can_manage_topics`

### 📂 Общий топик (single_topic)

**Преимущества:**
- Классический режим работы
- Не требует прав на управление топиками
- Подходит для обычных групп

**Требования:**
- ID топика в `.env` (опционально)
- Бот — администратор группы

## 📞 Поддержка

* **GitHub Issues** — баги и предложения

## 📄 Лицензия

MIT License — свободное использование и модификация проекта.

---

⭐️ Если этот проект оказался полезным, поставьте звезду на GitHub!
