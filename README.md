# Telegram Support Bot

Этот проект представляет собой Telegram-бота для организации простейшей системы поддержки.
Бот принимает сообщения пользователей (текст, фото, видео, аудио, документы, голосовые) и пересылает их в чат поддержки.
Операторы поддержки отвечают на сообщения прямо в общем чате, используя функцию Reply, а бот автоматически перенаправляет ответ тому пользователю, от которого пришёл запрос.

***Возможности :***

* **Двусторонняя связь:** Пересылка сообщений от пользователей в чат поддержки и обратно.
* **Поддержка медиафайлов:** Работает с текстом, фото, видео, документами, голосовыми и аудиосообщениями.
* **База данных:** Использует SQLite для хранения связей сообщений (чтобы бот знал, кому отправлять ответ).
* **Логирование:** Ведёт лог основных событий и ошибок.

## 🛠 Требования

* Python 3.8+
* Токен Telegram бота
* ID чата (группы), где будут находиться операторы поддержки

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/gniloeapple/telegram-Support-Bot.git
cd telegram-support-bot
```

### 2. Создание виртуального окружения (рекомендуется)

```bash
python -m venv venv
# Активация для Windows:
venv\Scripts\activate
# Активация для Linux/macOS:
source venv/bin/activate
```

### 3. Установка зависимостей

Установите необходимые библиотеки из файла `requirements.txt`:

```bash
pip install -r requirements.txt
```

### 4. Настройка окружения

Переименуйте файл `file.env` в `.env` (или создайте новый) и откройте его для редактирования:

```env
TELEGRAM_TOKEN=ваш_токен_бота
SUPPORT_CHAT_ID=-100xxxxxxxxxx
```

**Где взять данные:**

* **TELEGRAM_TOKEN**: Напишите [@BotFather](https://t.me/BotFather), создайте нового бота и скопируйте выданный токен.
* **SUPPORT_CHAT_ID**:

1. Создайте группу в Telegram, добавьте туда вашего бота и сделайте его администратором.
2. Напишите любое сообщение в эту группу.
3. Используйте стороннего бота (например, `@GetMyIDBot` или перешлите сообщение в `@userinfobot`), чтобы узнать ID этой группы.
4. Обязательно убедитесь, что ID начинается с `-100` (для супергрупп).

### 5. Запуск бота

```bash
python bot.py
```

## 📖 Как использовать

### Для пользователей

1. Пользователь запускает бота командой `/start`.
2. Пишет сообщение, отправляет фото или видео.
3. Бот пересылает это в чат поддержки.

### Для операторов поддержки

1. В чат поддержки (`SUPPORT_CHAT_ID`) приходит сообщение вида:
`📩 Сообщение от Иван (id: 12345): [Текст/Медиа]`
2. Чтобы ответить пользователю, оператор должен сделать **Reply (Ответить)** на это сообщение внутри группы.
3. Бот перешлет ответ оператора обратно пользователю в личные сообщения.

## ⚙️ Команды бота

* `/start` — Приветственное сообщение и режим работы.
* `/help` — Справка по оформлению обращений.

